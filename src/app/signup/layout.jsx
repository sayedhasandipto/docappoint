export const metadata = {
  title: "Register | DocAppoint",
  description: "Create a DocAppoint account to book and manage appointments with top doctors.",
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
