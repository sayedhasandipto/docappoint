export const metadata = {
  title: "All Appointments | DocAppoint",
  description: "Browse and search for the best doctors. Book an appointment easily through DocAppoint's comprehensive directory.",
};

export default function AppointmentLayout({ children }) {
  return <>{children}</>;
}
