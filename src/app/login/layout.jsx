export const metadata = {
  title: "Login | DocAppoint",
  description: "Login to your DocAppoint account to manage your appointments and profile.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
